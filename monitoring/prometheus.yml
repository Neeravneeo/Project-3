global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"

scrape_configs:
  - job_name: 'tradeai_backend'
    scrape_interval: 15s
    static_configs:
      - targets: ['api:8000']
